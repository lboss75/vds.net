<Window x:Class="IVySoft.VDS.Client.UI.WPF.Channel.CreateChannel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IVySoft.VDS.Client.UI.WPF.Channel"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Создать канал" Height="100" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label >Название:</Label>
        <TextBox Grid.Column="1" x:Name="nameEdit" Margin="5"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Margin="10" Padding="10" Width="100" x:Name="createBtn" IsDefault="True" Click="createBtn_Click" >Создать</Button>
        </StackPanel>
    </Grid>
</Window>
